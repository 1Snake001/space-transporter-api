let spaceship;_ad4‍.x([["default",()=>SpaceshipRepository]]);_ad4‍.w("../db",[["spaceship",["spaceship"],function(v){spaceship=v}]]);

               class SpaceshipRepository {
  static getTheShip() {
    return new Promise((resolve, reject) => {
      spaceship.findOne({_id: 1}, (err, spaceship) => {
        if (err) reject(err);
        resolve(spaceship);
      });
    });
  }

  static move(spaceshipId, planetId) {
    return new Promise((resolve, reject) => {
      spaceship.update({ _id: spaceshipId}, { $set: {planetId}}, {}, (err, numAffected) => {
        if (err) reject(err);
        resolve(numAffected);
      });
    });
  }

  static addPeople(spaceshipId, count) {
    return new Promise((resolve, reject) => {
      spaceship.update({ _id: spaceshipId}, { $inc: {passengers: count}}, {}, (err, numAffected) => {
        if (err) reject(err);
        resolve(numAffected);
      });
    });
  }

  static removePeople(spaceshipId, count) {
    return new Promise((resolve, reject) => {
      spaceship.update({ _id: spaceshipId}, { $inc: {passengers: -count}}, {}, (err, numAffected) => {
        if (err) reject(err);
        resolve(numAffected);
      });
    });
  }
}
